package pages.base_abstract;

import org.openqa.selenium.WebDriver;

public abstract class NewsHeaderPage extends BasePage {
    protected NewsHeaderPage(WebDriver driver) {
        super(driver);
    }
}
