package pages;

import org.openqa.selenium.WebDriver;
import pages.base_abstract.HeaderPage;

public class AboutPage extends HeaderPage {

    public AboutPage(WebDriver driver) {
        super(driver);
    }
}
