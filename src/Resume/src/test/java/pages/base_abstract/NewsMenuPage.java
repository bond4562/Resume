package pages.base_abstract;

import org.openqa.selenium.WebDriver;

public abstract class NewsMenuPage extends BasePage {
    protected NewsMenuPage(WebDriver driver) {
        super(driver);
    }
}
